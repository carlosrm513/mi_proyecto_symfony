<form method="post" action="{{ path('app_login_admin_delete', {'id': login_admin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ login_admin.id) }}">
    <button class="btn">Delete</button>
</form>
