<form method="post" action="{{ path('app_perfiles_admin_delete', {'idperfilesAdmin': perfiles_admin.idperfilesAdmin}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ perfiles_admin.idperfilesAdmin) }}">
    <button class="btn">Delete</button>
</form>
