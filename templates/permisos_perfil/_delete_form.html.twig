<form method="post" action="{{ path('app_permisos_perfil_delete', {'idpermisosPerfil': permisos_perfil.idpermisosPerfil}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ permisos_perfil.idpermisosPerfil) }}">
    <button class="btn">Delete</button>
</form>
