{% extends 'base.html.twig' %}

{% block title %}Perfil{% endblock %}

{% block body %}
    <h1>Perfil</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idperfil</th>
                <td>{{ perfil.idperfil }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ perfil.nombre }}</td>
            </tr>
            <tr>
                <th>FechaCreacion</th>
                <td>{{ perfil.fechaCreacion ? perfil.fechaCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_perfil_index') }}">back to list</a>

    <a href="{{ path('app_perfil_edit', {'idperfil': perfil.idperfil}) }}">edit</a>

    {{ include('perfil/_delete_form.html.twig') }}
{% endblock %}
